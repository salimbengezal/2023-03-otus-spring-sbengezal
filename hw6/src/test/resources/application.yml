spring:
  shell:
    interactive:
      enabled: false
  sql:
    init:
      mode: always
      data-locations: data.sql
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: validate
    show-sql: true

logging:
  level:
    ROOT: ERROR